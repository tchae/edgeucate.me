<li>
	<%= gravatar_for user, :size => 30 %>
	<%= link_to user.username + " | " + user.name, user %>
	<% if current_user.admin? %>
	| <%= link_to "delete user", user, :method => :delete, :confirm => "You sure?",
									   :title => "Delete #{user.username}" %>
	<% end %>
</li>